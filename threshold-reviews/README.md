# Threshold Reviews

Auto-generated weekly by `.github/workflows/weekly-threshold-tune.yml` (Sundays).
Each `YYYY-MM-DD.md` is the segment performance breakdown + any approval-threshold
changes applied that week. Thresholds themselves live in `config/approval-thresholds.json`.
